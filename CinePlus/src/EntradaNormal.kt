class EntradaNormal(
    codigo: String,
    precioBase: Double,
    sala: String,
    pelicula: String
) : Entrada(codigo, precioBase, sala, pelicula)
//queda igual ya que hereda y se ocupo 3 veces dentro del codigo main